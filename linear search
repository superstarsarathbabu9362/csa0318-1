# 4TH QUESTION
# LINEAR SEARCH
#include<stdio.h>
int main()
{
   int arr[]={44,55,66,77,88);
   int size=sizeof(arr)/sizeof(arr[0]);
   int key=44;
   for(int i=0;i<size;i++)
   {
       if(key==arr[i]
       {
           printf("position of key %d\n",key,i);
      }
  }
}
